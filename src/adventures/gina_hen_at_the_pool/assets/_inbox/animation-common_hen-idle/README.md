# Upload: idle

- Task id: `animation-common_hen-idle`
- Type: animation
- Gina standing, gentle idle (breathing / small bob).
- 2 frames · 120x120 each

**Drop here:** the 2 frame PNGs (120x120), in order — filenames only decide frame order, so name them e.g. 01.png, 02.png…

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/common/hen/idle.png` and archives the raw inputs under `_sources/animation-common_hen-idle/`.
