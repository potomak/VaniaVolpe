# Upload: walking

- Task id: `animation-common_hen-walking`
- Type: animation
- Gina walking cycle, side view (faces east; the engine mirrors it for west).
- 4 frames · 120x120 each

**Drop here:** the 4 frame PNGs (120x120), in order — filenames only decide frame order, so name them e.g. 01.png, 02.png…

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/common/hen/walking.png` and archives the raw inputs under `_sources/animation-common_hen-walking/`.
