# Upload: background

- Task id: `art-common_tree-background`
- Type: art
- The tree scene backdrop.
- 800x600

**Drop here:** the finished PNG (800x600) (any filename)

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/common/tree/background.png` and archives the raw inputs under `_sources/art-common_tree-background/`.
