# Upload: dive_again

- Task id: `voice-pool_dialog-dive_again`
- Type: voice
- Splashes in and wants to go again (adventure replays).
- Line to record: "Che bello! Ancora!"

**Drop here:** the recorded line as a WAV (any filename; saved as dive_again.wav)

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/it_IT/pool/dialog/dive_again.wav` and archives the raw inputs under `_sources/voice-pool_dialog-dive_again/`.
