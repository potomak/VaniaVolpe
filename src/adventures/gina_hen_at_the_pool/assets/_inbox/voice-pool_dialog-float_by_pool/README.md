# Upload: float_by_pool

- Task id: `voice-pool_dialog-float_by_pool`
- Type: voice
- Tries to dive while the float is still by the pool.
- Line to record: "Mi serve il salvagente: e' li' vicino alla piscina."

**Drop here:** the recorded line as a WAV (any filename; saved as float_by_pool.wav)

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/it_IT/pool/dialog/float_by_pool.wav` and archives the raw inputs under `_sources/voice-pool_dialog-float_by_pool/`.
