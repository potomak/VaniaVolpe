# Upload: float_in_tree

- Task id: `voice-pool_dialog-float_in_tree`
- Type: voice
- Tries to dive while the float is stuck in the tree.
- Line to record: "Il salvagente e' sull'albero! Devo recuperarlo."

**Drop here:** the recorded line as a WAV (any filename; saved as float_in_tree.wav)

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/it_IT/pool/dialog/float_in_tree.wav` and archives the raw inputs under `_sources/voice-pool_dialog-float_in_tree/`.
