# Upload: got_goggles

- Task id: `voice-pool_dialog-got_goggles`
- Type: voice
- Collects the goggles.
- Line to record: "Ho preso gli occhialini!"

**Drop here:** the recorded line as a WAV (any filename; saved as got_goggles.wav)

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/it_IT/pool/dialog/got_goggles.wav` and archives the raw inputs under `_sources/voice-pool_dialog-got_goggles/`.
