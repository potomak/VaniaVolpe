# Upload: sunscreen_done

- Task id: `voice-pool_dialog-sunscreen_done`
- Type: voice
- Taps the bottle again after applying sunscreen.
- Line to record: "Ho gia' messo la crema."

**Drop here:** the recorded line as a WAV (any filename; saved as sunscreen_done.wav)

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/it_IT/pool/dialog/sunscreen_done.wav` and archives the raw inputs under `_sources/voice-pool_dialog-sunscreen_done/`.
