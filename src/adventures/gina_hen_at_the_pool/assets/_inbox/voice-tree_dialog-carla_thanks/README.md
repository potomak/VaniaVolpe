# Upload: carla_thanks

- Task id: `voice-tree_dialog-carla_thanks`
- Type: voice
- Carla takes the grapes and returns the float.
- Line to record: "Mmm, che uva buona! Ecco il tuo salvagente!"

**Drop here:** the recorded line as a WAV (any filename; saved as carla_thanks.wav)

Then run `tools/consolidate_assets.py` — it moves the files to `src/adventures/gina_hen_at_the_pool/assets/it_IT/tree/dialog/carla_thanks.wav` and archives the raw inputs under `_sources/voice-tree_dialog-carla_thanks/`.
